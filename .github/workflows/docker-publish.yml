name: Publish Docker Image

# Builds and publishes the painapple-code image to Docker Hub.
#
# Versioning scheme: SemVer — git tags shaped like vMAJOR.MINOR.PATCH
# (e.g. v0.1.0, v1.2.3, v1.2.3-rc1). The git tag is the single source of
# truth — both the Docker image tag and the pip package version
# (via setuptools-scm) derive from it.
#
# Triggers:
#   - push of a tag matching v*.*.*  → publishes :<tag> and :latest
#                                       (pre-release tags like v0.1.0-rc1
#                                       publish :<tag> and :rc, and move
#                                       :latest only until the first stable
#                                       release exists to hold it)
#   - workflow_dispatch, tag input   → re-runs the FULL tag pipeline for an
#                                       existing tag, without any git
#                                       operation. This is the recovery path
#                                       when a release half-failed: re-pushing
#                                       the tag would re-trigger PyPI too.
#   - workflow_dispatch, no input    → publishes :edge (does not move :latest)
#
# Required repository secrets:
#   - DOCKERHUB_USERNAME   (wrotek)
#   - DOCKERHUB_TOKEN      (Hub access token, NOT account password)
#
# Multi-arch: builds linux/amd64 + linux/arm64 via buildx + QEMU.
#
# Job graph: test → build-stage → smoke-test → promote. The build pushes
# only an ephemeral :staging-<run_id> tag; the smoke test verifies that
# digest; promote re-tags the verified digest onto the public tags. A
# failure anywhere before promote publishes nothing a user can pull.

on:
    push:
        tags:
            - "v*.*.*"
    workflow_dispatch:
        inputs:
            tag:
                description: >-
                    Existing release tag to (re)build and publish
                    (e.g. v1.0.0-rc37). Leave empty for an :edge build
                    off the current branch.
                required: false
                type: string

env:
    IMAGE: docker.io/wrotek/painapple-code

jobs:
    # Release gate: the full security regression suite must pass on the
    # tree being shipped before anything is built or published. The ref
    # passthrough matters for dispatch-with-tag recovery runs — without it
    # the gate would test the branch, not the tag being rebuilt.
    test:
        uses: ./.github/workflows/tests.yml
        with:
            ref: ${{ inputs.tag || '' }}

    # Builds the multi-arch image and pushes it ONLY to an ephemeral
    # :staging-<run_id> tag. Nothing public moves here — the version tag,
    # :latest, :rc and :edge are all assigned by `promote`, after the smoke
    # test has verified this exact digest. (buildx can't --load a
    # multi-arch build for local testing, so push-to-staging is the only
    # way to test before promoting.)
    build-stage:
        needs: test
        runs-on: ubuntu-latest
        permissions:
            contents: read
        outputs:
            tag: ${{ steps.relref.outputs.tag }}
            prerelease: ${{ steps.relref.outputs.prerelease }}
            stable: ${{ steps.rel.outputs.stable }}
            digest: ${{ steps.build.outputs.digest }}
        steps:
            # fetch-depth: 0 so setuptools-scm can `git describe` against
            # all tags. Default shallow clone hides everything but HEAD.
            # ref: a dispatch with a tag input builds THAT tag's tree; a tag
            # push or bare dispatch checks out the triggering ref as before.
            - uses: actions/checkout@v4
              with:
                  fetch-depth: 0
                  ref: ${{ inputs.tag || github.ref }}

            # One canonical "which release is this?" answer, instead of every
            # step re-deriving it from github.ref (which is wrong for a
            # dispatch-with-tag run). Empty tag == :edge build.
            - name: Resolve release tag
              id: relref
              run: |
                  if [[ "${{ github.event_name }}" == "push" ]]; then
                      tag="${GITHUB_REF#refs/tags/}"
                  else
                      tag="${{ inputs.tag }}"
                  fi
                  if [[ -n "$tag" && ! "$tag" =~ ^v[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.]+)?$ ]]; then
                      echo "✗ '$tag' is not a vX.Y.Z[-pre] release tag"
                      exit 1
                  fi
                  prerelease=false
                  [[ "$tag" == *-* ]] && prerelease=true
                  echo "tag=$tag" >> "$GITHUB_OUTPUT"
                  echo "prerelease=$prerelease" >> "$GITHUB_OUTPUT"
                  echo "→ release tag: '${tag:-<none — edge build>}' (prerelease: $prerelease)"

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: "3.12"

            # Single source-of-truth version. Same value gets baked into
            # the wheel (via SETUPTOOLS_SCM_PRETEND_VERSION_FOR_… inside
            # the image) and surfaces as `painapple --version`.
            - name: Resolve package version from git
              id: scm
              run: |
                  pip install --quiet 'setuptools-scm>=8'
                  version=$(python -m setuptools_scm)
                  echo "version=$version" >> "$GITHUB_OUTPUT"
                  echo "→ setuptools-scm resolved: $version"

            # "Skip :latest for pre-releases" is only correct once a stable
            # release exists to point it at. Before that it freezes :latest
            # on whatever happened to publish it first — which is exactly
            # what happened here: v1.0.0-rc1 shipped :latest under the old
            # latest=auto flavor, the very next commit turned pre-release
            # :latest off, and `painapple pull` served that one build for a
            # month while rc2…rc16 came and went unseen.
            - name: Has a stable release ever shipped?
              id: rel
              run: |
                  if git tag -l 'v*' | grep -Eq '^v[0-9]+\.[0-9]+\.[0-9]+$'; then
                      echo "stable=true" >> "$GITHUB_OUTPUT"
                      echo "→ stable release exists — pre-releases will NOT move :latest"
                  else
                      echo "stable=false" >> "$GITHUB_OUTPUT"
                      echo "→ no stable release yet — pre-releases move :latest"
                  fi

            - name: Set up QEMU
              uses: docker/setup-qemu-action@v3

            - name: Set up Buildx
              uses: docker/setup-buildx-action@v3

            - name: Log in to Docker Hub
              uses: docker/login-action@v3
              with:
                  username: ${{ secrets.DOCKERHUB_USERNAME }}
                  password: ${{ secrets.DOCKERHUB_TOKEN }}

            # Only the ephemeral staging tag is assigned at build time. The
            # public tag policy lives in the `promote` job below. The OCI
            # labels ARE baked here — they ride the manifest through the
            # digest promotion unchanged.
            #
            # flavor latest=false: the default (latest=auto) would mark
            # tag-ish builds as :latest with no semver awareness; nothing in
            # this workflow may assign :latest except promote.
            - name: Derive staging tag + OCI labels
              id: meta
              uses: docker/metadata-action@v5
              with:
                  images: ${{ env.IMAGE }}
                  flavor: |
                      latest=false
                  tags: |
                      type=raw,value=staging-${{ github.run_id }}
                  # metadata-action ALSO auto-derives description/licenses/url from
                  # the GitHub repo's About field + detected SPDX id, and everything
                  # it emits is passed to build-push-action as --label below, which
                  # beats the Dockerfile's own LABEL. So the Dockerfile's description
                  # and licenses were dead: shipped images carried the About text and
                  # GitHub's `AGPL-3.0` instead. Pin both here — an explicit entry in
                  # this block does win over the auto-derived one.
                  labels: |
                      org.opencontainers.image.description=Self-hosted web client and PWA for Claude Code sessions
                      org.opencontainers.image.licenses=AGPL-3.0-or-later
                      org.opencontainers.image.version=${{ steps.scm.outputs.version }}
                      org.opencontainers.image.revision=${{ github.sha }}
                      org.opencontainers.image.created=${{ github.event.repository.updated_at }}

            # Built on the runner, not in the image: the Dockerfile COPYs the
            # working tree, and .dockerignore allowlists src/, so a present
            # static/dist/ ships. Doing it here also keeps it out of the
            # per-architecture builds — the bundle is plain JS, arch-agnostic.
            - name: Set up Node
              uses: actions/setup-node@v4
              with:
                  node-version: "20"

            # Same script a from-source deploy runs, so there is one build path
            # and CI exercises it. Installs only esbuild + yaml (~7 MB).
            - name: Build frontend bundle
              run: ./build-frontend.sh

            - name: Build and push
              id: build
              uses: docker/build-push-action@v6
              with:
                  context: .
                  platforms: linux/amd64,linux/arm64
                  push: true
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}
                  build-args: |
                      SCM_VERSION=${{ steps.scm.outputs.version }}
                  cache-from: type=gha
                  cache-to: type=gha,mode=max

    smoke-test:
        # Pulls the staged image BY DIGEST on a clean runner and verifies
        # the bridge boots: /health must return HTTP 200 within 30 s. At
        # this point no public tag has moved — a failure here leaves
        # :latest/:rc/:<version> exactly where they were. Runs only for
        # amd64 — buildx publishes the same code for both arches, so one
        # platform is sufficient to catch packaging regressions.
        needs: build-stage
        runs-on: ubuntu-latest
        steps:
            - name: Boot container and curl /health
              run: |
                  set -euo pipefail
                  IMG="${{ env.IMAGE }}@${{ needs.build-stage.outputs.digest }}"
                  echo "→ Pulling $IMG"
                  docker pull "$IMG"

                  # Workspace mount: the entrypoint refuses to start unless
                  # /workspace is non-empty. Use a one-file dir as a stand-in.
                  mkdir -p /tmp/ws && echo "smoke" > /tmp/ws/README

                  echo "→ Starting smoke container"
                  docker run -d --name papple-smoke \
                      -p 127.0.0.1:8765:8765 \
                      -v /tmp/ws:/workspace \
                      "$IMG"

                  # The container binds 0.0.0.0, so TLS auto-mode is ON with a
                  # self-signed cert — https + -k. Keep a plain-http fallback in
                  # case the default TLS behavior ever changes.
                  echo "→ Waiting for /health"
                  for i in $(seq 1 30); do
                      if curl -fsSk https://127.0.0.1:8765/health > /dev/null 2>&1 \
                          || curl -fsS http://127.0.0.1:8765/health > /dev/null 2>&1; then
                          echo "✓ /health responded after ${i}s"
                          # Also confirm the version we shipped is the one we baked in.
                          docker exec papple-smoke python -c \
                              "import painapple_code; print('version:', painapple_code.__version__)"
                          exit 0
                      fi
                      sleep 1
                  done

                  echo "✗ /health never responded"
                  docker logs papple-smoke || true
                  exit 1

            - name: Cleanup
              if: always()
              run: docker rm -f papple-smoke 2>/dev/null || true

    promote:
        # The ONLY place public tags move. `imagetools create` is a pure
        # manifest copy — no rebuild — so what ships is bit-for-bit the
        # digest the smoke test verified, and re-running this job cannot
        # change an existing version's digest.
        #
        # Tag policy (driven by build-stage outputs, so a dispatch-with-tag
        # run behaves exactly like the original tag push):
        #   release tag v0.1.0         → :v0.1.0, :latest
        #   release tag v0.1.0-rc1     → :v0.1.0-rc1, :rc
        #                                (+ :latest only while NO stable
        #                                 release exists — see build-stage)
        #   no release tag (dispatch)  → :edge          (never moves :latest)
        #
        # :rc is the moving pre-release pointer — it keeps working after
        # v1.0.0 ships, when :latest correctly stops tracking rcs. The
        # version tag keeps the leading `v`; we deliberately emit no :0.1 /
        # :0 semver aliases — library ergonomics an end-user app doesn't
        # want.
        needs: [build-stage, smoke-test]
        runs-on: ubuntu-latest
        steps:
            - name: Log in to Docker Hub
              uses: docker/login-action@v3
              with:
                  username: ${{ secrets.DOCKERHUB_USERNAME }}
                  password: ${{ secrets.DOCKERHUB_TOKEN }}

            - name: Promote verified digest to public tags
              run: |
                  set -euo pipefail
                  digest="${{ needs.build-stage.outputs.digest }}"
                  tag="${{ needs.build-stage.outputs.tag }}"
                  prerelease="${{ needs.build-stage.outputs.prerelease }}"
                  stable="${{ needs.build-stage.outputs.stable }}"

                  args=()
                  if [[ -n "$tag" ]]; then
                      args+=(-t "$IMAGE:$tag")
                      if [[ "$prerelease" == "true" ]]; then
                          args+=(-t "$IMAGE:rc")
                          if [[ "$stable" == "false" ]]; then
                              args+=(-t "$IMAGE:latest")
                          fi
                      else
                          args+=(-t "$IMAGE:latest")
                      fi
                  else
                      args+=(-t "$IMAGE:edge")
                  fi

                  echo "→ promoting $digest to:"
                  printf '   %s\n' "${args[@]}"
                  docker buildx imagetools create "${args[@]}" "$IMAGE@$digest"

            # Housekeeping, not correctness: drop the ephemeral staging tag.
            # Tag deletion goes through the Hub account API, which a
            # Read & Write PAT may refuse (403) — hence continue-on-error; a
            # lingering staging-* tag is harmless and can be pruned by hand.
            - name: Delete staging tag
              continue-on-error: true
              run: |
                  repo="${IMAGE#docker.io/}"
                  token=$(curl -fsS -H "Content-Type: application/json" \
                      -d '{"username": "${{ secrets.DOCKERHUB_USERNAME }}", "password": "${{ secrets.DOCKERHUB_TOKEN }}"}' \
                      https://hub.docker.com/v2/users/login \
                      | python3 -c "import sys, json; print(json.load(sys.stdin)['token'])")
                  curl -fsS -X DELETE -H "Authorization: JWT $token" \
                      "https://hub.docker.com/v2/repositories/$repo/tags/staging-${{ github.run_id }}/"
                  echo "→ staging-${{ github.run_id }} deleted"

# The Docker Hub Overview tab is synced by dockerhub-readme.yml, which
# tracks main rather than release tags. It lived here as a job gated on
# stable tags — a gate that never once opened, since the first tag ever
# pushed was a pre-release. See that workflow's header.
