# Changelog

Notable changes per release. Versions follow [SemVer](https://semver.org/); the git tag is the single source of truth — the PyPI wheel, the Docker image tag and the app's own `__version__` all derive from it. Release artifacts: [PyPI](https://pypi.org/project/painapple-code/), [Docker Hub](https://hub.docker.com/r/wrotek/painapple-code), [GitHub Releases](https://github.com/wrotek/painapple-code/releases).

--8<-- "CHANGELOG.md:5"
